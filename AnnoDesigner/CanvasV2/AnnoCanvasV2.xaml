<UserControl x:Class="AnnoDesigner.CanvasV2.AnnoCanvasV2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600" 
             ClipToBounds="True"
             Focusable="True">
    <UserControl.ContextMenu>
        <ContextMenu Opened="ContextMenu_Opened">
            <MenuItem Header="_Copy" Command="{Binding CopyCommand}" InputGestureText="Ctrl+C"/>
            <MenuItem Header="_Paste" Command="{Binding PasteCommand}" InputGestureText="Ctrl+V"/>
            <MenuItem Header="_Delete" Command="{Binding DeleteCommand}" InputGestureText="Del"/>
            <Separator/>
            <MenuItem Header="_Rotate" Command="{Binding RotateCommand}" InputGestureText="R"/>
            <MenuItem Header="Select Same Identifier" Command="{Binding SelectSameIdentifierCommand}" InputGestureText="Ctrl+Double Click"/>
            <Separator/>
            <MenuItem Header="_Undo" Command="{Binding UndoCommand}" InputGestureText="Ctrl+Z"/>
            <MenuItem Header="_Redo" Command="{Binding RedoCommand}" InputGestureText="Ctrl+Y"/>
        </ContextMenu>
    </UserControl.ContextMenu>
</UserControl>